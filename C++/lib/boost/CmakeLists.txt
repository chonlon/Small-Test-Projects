cmake_minimum_required(VERSION 3.0.2)
project(boost)

find_path(BOOST_INCLUDE_DIRS "boost/any.")

add_executable(hex ${CMAKE_CURRENT_LIST_DIR}/algorithm/hex.cpp) 
target_include_directories(hex PRIVATE ${BOOST_INCLUDE_DIRS})

add_executable(equal1 "${CMAKE_CURRENT_LIST_DIR}/algorithm/equal.cpp") 
target_include_directories(equal1 PRIVATE ${BOOST_INCLUDE_DIRS})

add_executable(increasing_decreasing "${CMAKE_CURRENT_LIST_DIR}/algorithm/increasing_decreasing.cpp") 
target_include_directories(increasing_decreasing PRIVATE ${BOOST_INCLUDE_DIRS})

add_executable(itoa "${CMAKE_CURRENT_LIST_DIR}/algorithm/itoa.cpp")
target_include_directories(itoa PRIVATE ${BOOST_INCLUDE_DIRS})

add_executable(clamp "${CMAKE_CURRENT_LIST_DIR}/algorithm/clamp.cpp")
target_include_directories(clamp PRIVATE ${BOOST_INCLUDE_DIRS})

add_executable(palindrome "${CMAKE_CURRENT_LIST_DIR}/algorithm/palindrome.cpp")
target_include_directories(palindrome PRIVATE ${BOOST_INCLUDE_DIRS})


